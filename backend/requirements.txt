# ============================================
# 金融多智能体协作系统 - Python 依赖
# 版本锁定日期: 2026-06-16
# ============================================

# --- Web 框架 ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sse-starlette>=2.0.0

# --- Agent 框架 ---
# LangGraph 1.x 稳定版 + LangChain 1.x（同步升级，互兼容）
langgraph>=1.2.0
langchain>=1.3.0
langchain-community>=0.4.0

# --- LLM 客户端 ---
openai>=1.30.0          # DeepSeek / Qwen OpenAI 兼容接口

# --- 向量数据库 ---
pymilvus>=2.4.0,<2.5.0  # 与 docker compose 中 Milvus 2.4 对齐

# --- 数据库驱动 ---
sqlalchemy[asyncio]>=2.0.30
aiomysql>=0.2.0
pymysql>=1.1.0

# --- 缓存与队列 ---
# Celery 5.6 仅兼容 redis-py <=5.2.1，不能升到 6.x/8.x
celery[redis]>=5.6.0
redis>=5.0.0,<=5.2.1

# --- 数据处理 ---
pandas>=2.2.0
numpy>=1.26.0

# --- PDF 解析 ---
pymupdf>=1.24.0
pdfplumber>=0.10.0

# --- 日志 ---
structlog>=24.0.0

# --- 工具 ---
pydantic>=2.7.0,<3.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
